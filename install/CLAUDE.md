# Project AI Instructions

Read `AIDL-SYSTEM.md` to learn the AIDL format.
Then read `project.aidl` for the complete system architecture.

When working on this project:
- Consult `project.aidl` before making architectural decisions
- Update `project.aidl` when the architecture changes
- Use AIDL notation in your reasoning when describing the system
